# Sandbox agent instructions

- This repository is disposable and contains no production code or data.
- Make only the change requested by the active GitHub issue.
- Run the verification command in `.metis.yml` before reporting completion.
- Open a pull request; never merge it.
- Never deploy or access production systems.
- If a required decision or detail is missing, stop and respond with `BLOCKED:` followed by one concrete question.
